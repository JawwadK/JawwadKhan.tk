var typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    typeSpeed: 60,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1250,
    loop: true,
    loopCount: Infinity,
  });